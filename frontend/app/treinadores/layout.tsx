import TrainersPage from './page';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Hall da Fama',
  description: 'Veja os Pokémons favoritos de todos os treinadores registrados no PokeCenter.',
};

export default TrainersPage;
